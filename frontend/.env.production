# Production build config for 7kc.com — SPA at the root, API under /api on the
# same domain. The API base stays relative so the built app calls
# https://7kc.com/api/v1/... whatever host it is served from (no CORS needed).
VITE_API_URL=/api/v1
